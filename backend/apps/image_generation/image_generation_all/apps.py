from django.apps import AppConfig

class ImageGenerationAllConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.image_generation.image_generation_all'
    label = 'image_generation_all'
